<div class='container px-5 px-sm-0'>
 <div class='row'>
  <div class='col-md-3'>

<%= render 'info', user: @user, book: @book %>
</div>
<div class='col-md-8 offset-md-1' >



 <% @books.each do |book| %>
  <tr>
  <td><%= image_tag book.user.get_profile_image(100,100) %>
  <%= link_to user_path (book.user_id) %>
  </td>
 </td>
  <td>name <%= book.user.name %></td>

  <td><%= link_to book.title, book_path(book) %></td>
  <td><%= book.body %></td>
  </tr>

  <% end %>

</div>
</div>
</div>
