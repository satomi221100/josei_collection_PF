<div class='container px-5 px-sm-0'>
 <div class='row'>
  <div class='col-md-3'>
<%= render 'users/info', user: @user,book: @book%>
</div>
<div class='col-md-8 offset-md-1' >

<h2>user</h2>
  <table class='table table-hover table-inverse'>
  <thead>
    <tr>
      <th></th>
      <th>ユーザー名</th>
      <th>BOOK</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>

<% @users.each do |user| %>
  <tr>
  <td><%= image_tag user.get_profile_image(100,100) %></td>

  <td> <%= user.name %></td>
  <td><%= link_to "見る", user_path(user.id), class: 'btn btn-outline-dark' %></td>
  </tr>
<% end %>

</tbody>
</table>
</div>